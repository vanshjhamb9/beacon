# Relationship Graph (AIP)

Append-only graph: Company → Decision Makers → Departments → Technology → Campaigns → Emails → Replies → Meetings → Proposals → Revenue → Referrals → History.

Tables: `relationship_graph_nodes`, `relationship_graph_edges` (immutable).
