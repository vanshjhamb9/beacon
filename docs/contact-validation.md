# Contact Validation (AIP)

Never invent contacts. Observed contacts only.

Validates business email format, domain match, optional MX interface, role validation, public presence, business phone format/country code, freshness, conflicts, and confidence.

Invalid emails/phones are discarded — never rewritten into guesses.
