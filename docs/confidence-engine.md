# Confidence Engine (AIP)

Every field is a `FieldValue` with confidence (0–100), source, last verified, evidence, and conflicts.

Never overwrite history — append-only verification/field_sources records.
